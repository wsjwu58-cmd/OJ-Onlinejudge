server:
  port: 8080

spring:
  application:
    name: oj-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: 192.168.141.129:8848
      username: nacos
      password: nacos
      discovery:
        namespace: public
      config:
        namespace: public
        file-extension: yml
        shared-configs:
          - data-id: oj-common-dev.yml
            refresh: true
